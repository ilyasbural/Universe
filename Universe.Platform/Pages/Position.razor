@page "/position"
@inject HttpClient Http

<h3>Position</h3>

@code
{
    private PositionResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<PositionResponse>("https://localhost:7255/api/position");
    }
}