@page "/country"
@inject HttpClient Http

<h3>Country</h3>

@code
{
    private CountryResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<CountryResponse>("https://localhost:7255/api/country");
    }
}