@page "/usernetwork"
@inject HttpClient Http

<h3>UserNetwork</h3>

@code
{
    private UserNetworkResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserNetworkResponse>("https://localhost:7255/api/usernetwork");
    }
}