@page "/userdetail"
@inject HttpClient Http

<h3>UserDetail</h3>

@code
{
    private UserDetailResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserDetailResponse>("https://localhost:7255/api/userdetail");
    }
}