@page "/emoji"
@inject HttpClient Http

<h3>Emoji</h3>

@code
{
    private EmojiResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<EmojiResponse>("https://localhost:7255/api/emoji");
    }
}