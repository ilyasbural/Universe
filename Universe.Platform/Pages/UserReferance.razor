@page "/userreferance"
@inject HttpClient Http

<h3>UserReferance</h3>

@code
{
    private UserReferanceResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserReferanceResponse>("https://localhost:7255/api/userreferance");
    }
}