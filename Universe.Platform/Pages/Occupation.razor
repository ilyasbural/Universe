@page "/occupation"
@inject HttpClient Http

<h3>Occupation</h3>

@code
{
    private OccupationResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<OccupationResponse>("https://localhost:7255/api/occupation");
    }
}