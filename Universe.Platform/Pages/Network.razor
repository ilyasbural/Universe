@page "/network"
@inject HttpClient Http

<h3>Network</h3>

@code
{
    private NetworkResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<NetworkResponse>("https://localhost:7255/api/network");
    }
}