@page "/usercertificate"
@inject HttpClient Http

<h3>UserCertificate</h3>

@code
{
    private UserCertificateResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserCertificateResponse>("https://localhost:7255/api/usercertificate");
    }
}