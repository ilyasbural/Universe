@page "/userabout"
@inject HttpClient Http

<h3>UserAbout</h3>

@code
{
    private UserAboutResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserAboutResponse>("https://localhost:7255/api/userabout");
    }
}