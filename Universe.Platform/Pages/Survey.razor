@page "/survey"
@inject HttpClient Http

<h3>Survey</h3>

@code
{
    private SurveyResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<SurveyResponse>("https://localhost:7255/api/survey");
    }
}