@page "/usercountry"
@inject HttpClient Http

<h3>UserCountry</h3>

@code
{
    private UserCountryResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserCountryResponse>("https://localhost:7255/api/usercountry");
    }
}