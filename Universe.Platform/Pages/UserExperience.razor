@page "/userexperience"
@inject HttpClient Http

<h3>UserExperience</h3>

@code
{
    private UserExperienceResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserExperienceResponse>("http://192.168.1.100/api/administrator?id=2&option=5");
    }
}