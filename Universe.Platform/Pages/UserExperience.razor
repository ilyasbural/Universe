@page "/userexperience"
@inject HttpClient Http

<h3>UserExperience</h3>

@code
{
    private UserExperienceResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserExperienceResponse>("https://localhost:7255/api/userexperience");
    }
}