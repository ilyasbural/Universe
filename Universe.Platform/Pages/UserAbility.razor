@page "/userability"
@inject HttpClient Http

<h3>UserAbility</h3>

@code
{
    private UserAbilityResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserAbilityResponse>("https://localhost:7255/api/userability");
    }
}