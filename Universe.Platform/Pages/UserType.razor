@page "/usertype"
@inject HttpClient Http

<h3>UserType</h3>

@code
{
    private UserTypeResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<UserTypeResponse>("https://localhost:7255/api/usertype");
    }
}