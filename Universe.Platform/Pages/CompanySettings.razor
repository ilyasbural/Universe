@page "/companysettings"
@inject HttpClient Http

<h3>CompanySettings</h3>

@code
{
    private CompanySettingsResponse Response;
    protected override async Task OnInitializedAsync()
    {
        Response = await Http.GetFromJsonAsync<CompanySettingsResponse>("https://localhost:7255/api/companysettings");
    }
}